package com;

/** represents canvas features that we have to integrate ourselves (using the canvas api)
 * instead of the KSU Canvas API, because some features just do not seem possible (or correct) in the current KSU API.
**/
public class CanvasGetterCustom {
    
}
